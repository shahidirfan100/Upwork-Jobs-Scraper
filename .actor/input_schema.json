{
    "title": "Upwork Jobs Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "searchUrl": {
            "title": "Search URL",
            "type": "string",
            "description": "Direct Upwork job search URL to scrape. Example: https://www.upwork.com/freelance-jobs/web-scraping/",
            "editor": "textfield",
            "prefill": "https://www.upwork.com/freelance-jobs/web-scraping/",
            "default": "https://www.upwork.com/freelance-jobs/web-scraping/"
        },
        "maxJobs": {
            "title": "Maximum Jobs",
            "type": "integer",
            "description": "Maximum number of job listings to scrape (0 = unlimited). Default is 20 for optimal performance.",
            "default": 20,
            "minimum": 0,
            "maximum": 10000
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings for bypassing anti-bot protection. Recommended for reliable scraping.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true
            }
        }
    },
    "required": [
        "searchUrl"
    ]
}